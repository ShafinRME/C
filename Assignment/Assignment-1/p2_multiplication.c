#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main()
{

    long long int a, b, mult;

    scanf("%lld %lld", &a, &b);
    mult = a * b;
    printf("%lld\n", mult);
    return 0;
}